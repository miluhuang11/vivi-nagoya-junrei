# Vivi 聖地巡禮網站｜需求紀錄

## 專案基本需求
- 做一個旅遊行程網站，推到 GitHub，用網址就能直接打開
- 可以自行新增／編輯／刪除景點，內容要能保存下來
- 資料庫選用 **Supabase**（雲端共用資料庫，多人打開看到同一份、即時同步）

## 功能與設計調整（依提出順序）

1. 手機版瀏覽為主，優化 UI、加上表情符號
2. 想要真實背景圖 → 後來先跳過找圖
3. 點行程項目可以直接打開 Google 地圖
4. 不需要多人共編，只要能自己改、改了會保存就好
5. 新增景點的視窗要能關掉，不要卡住畫面（修正：`hidden` 屬性被 CSS 蓋掉的 bug）

7. 時間要更細節（改成具體 HH:MM，而不是「上午/下午」）
8. 中文文字要精簡一點
9. 畫面設計參考 `https://miluhuang11.github.io/milutrip2026/` 的暖色系手機 App 風格
10. 行程要「每天各自一頁」，點日期切換，不要整頁捲動

12. 每天要有不同顏色 + 找風景照片當背景
13. 新增行程要能自動照時間排序插入
14. 標題要改成「**Vivi 聖地巡禮**」（不是參考網站的 MILU）
15. 新增行程要能自動照時間排序插入（`period` 用 24 小時制 HH:MM，改用 period 排序）
16. 打開網站當天如果剛好在行程範圍內，自動跳到當天那一頁
17. 加一道簡單的「編輯密碼」門檻，避免連結被轉傳出去後被陌生人亂改（密碼：`0619`，存在 `app.js` 的 `EDIT_PIN`，只存在瀏覽器本機 localStorage，非正式帳號系統）
18. 搜尋改成跨全部天數搜尋，不只搜目前顯示的那一天
19.（先擱置，之後補內容）重要資訊摘要卡片：航班編號／飯店地址電話／行李規定

## 技術重點備忘
- 網站：`index.html` / `style.css` / `app.js` / `seed-data.js`（純前端，無框架）
- 資料庫：Supabase 專案 `ujxaabxlmqlpreyijdxy`
  - `days` 表：每天的日期／標題／住宿
  - `items` 表：`period`(時間) / `name`(標題) / `text`(備註,選填) / `category` / `map_query` / 用 `period` 排序
- GitHub repo：`miluhuang11/vivi-nagoya-junrei`，GitHub Pages 部署
- 網址：https://miluhuang11.github.io/vivi-nagoya-junrei/
